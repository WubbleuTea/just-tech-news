<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
                                {{!-- checkout post-info for comments on helper functions --}}
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>